TELEGRAM_BOT_TOKEN=your-bot-token-here
ADMIN_CHAT_ID=your-telegram-user-id
ANTHROPIC_API_KEY=your-anthropic-api-key  # optional — omit to use Claude subscription (OAuth via `claude login`)
USE_SUBSCRIPTION=false                    # set to true to force OAuth even when ANTHROPIC_API_KEY is present
GROQ_API_KEY=your-groq-api-key        # optional — for voice message transcription
ASYNC_FEEDBACK=false                   # set to true to skip heartbeat/tool updates (status shown once, result sent when done)
CLAUDE_LOG_COMMANDS=0                  # set to 1 to log all Bash commands to ~/.claude/command-log.txt
PROJECTS_DIR=/home/youruser/Documents  # directory mounted into container for project access
